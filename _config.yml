# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.
title: Is Arch Broken?
email: callme5long@gmail.com
description: >- # this means to ignore newlines until "baseurl:"
  Is Arch Linux broken or just me?
baseurl: "/is-arch-broken" # the subpath of your site, e.g. /blog
url: "https://5long.github.io" # the base hostname & protocol for your site, e.g. http://example.com
twitter_username: 5long
github_username:  5long

permalink: /:title:output_ext

# Build settings
markdown: kramdown
remote_theme: mmistakes/jekyll-theme-basically-basic
plugins:
  - jekyll-feed

# Exclude from processing.
# The following items will not be processed, by default. Create a custom list
# to override the default setting.
# exclude:
#   - Gemfile
#   - Gemfile.lock
#   - node_modules
#   - vendor/bundle/
#   - vendor/cache/
#   - vendor/gems/
#   - vendor/ruby/
